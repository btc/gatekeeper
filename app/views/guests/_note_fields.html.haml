.nested-fields
  .row-fluid
    .span6
      = f.text_area :body, label: false, class: 'note-body span12'
    .span6
      = link_to_remove_association "remove note", f,
        confirm: 'Are you sure? This action cannot be undone', as: :button
      = f.error :body, class: 'alert alert-error'
