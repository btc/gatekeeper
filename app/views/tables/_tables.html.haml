%table.table.table-condensed
  %caption
    table openings on
    %strong= date.try(:strftime, '%A %e %B')
  %tr
    %th actions
    %th #
    %th name

  - @tables.each do |table|
    - free = table.free_on(date)
    %tr{ green_if(free) }
      %td
        - if free
          = link_to new_reservation_path(table: table, date: date), class: 'btn btn-mini' do
            %i.icon-plus
            %i.icon-book
        - else
          = link_to reservation_path(table.reservation_on(date)), class: 'btn btn-mini' do
            show
            %i.icon-book

      %td
        = best_in_place table, :number

      %td
        = best_in_place table, :name
        = link_to table do
          %button.btn.btn-mini
            &rarr;

:coffeescript
  jQuery ->
    $('.best_in_place').best_in_place()
